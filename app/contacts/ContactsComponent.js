export const metadata = {
  title: "Contacts", 
  description: "This is the contacts page", 
};

export default function ContactsComponent() {
  

    return (
      <div>
          ContactsPage
      </div>
    );
  }
  